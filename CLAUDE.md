# freshrss-cli

CLI tool for FreshRSS (Google Reader API compatible).

## Development
bun install
cp .env.example .env  # Edit with credentials
bun run src/main.ts feed list

## Install globally
bun link
